# QuePilot.ai — Technical Design Document (TDD)

**Status:** Draft v1
**Scope:** MVP architecture (lean stack) + target enterprise architecture
**Last updated:** 2026-09-15

## 1. Design Approach

The vision doc's long-term stack (Next.js frontend, .NET API, Azure SQL, Azure Blob Storage, Azure OpenAI, Entra ID, Redis, Application Insights) is the right target for an enterprise-grade, security-vendor-neutral product. But building all of it before validating that consultants will pay is the wrong sequencing risk for an MVP.

This TDD defines:
- **MVP architecture** — ships fast, proves the core loop (intake → AI generation → exportable deliverable), minimizes infra surface area.
- **Target architecture** — where we grow to once the product/market fit is validated and enterprise/federal customers need Azure-native compliance posture, SSO via Entra ID, and dedicated backend scaling.

Module boundaries (below) are designed so the MVP backend logic can be lifted into a dedicated API service later without a rewrite of business logic — only the transport/hosting layer changes.

## 2. MVP Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        Next.js App (Vercel)                   │
│  ┌───────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │  App Router    │  │  Route Handlers  │  │  Server Actions │ │
│  │  (UI, forms)   │  │  (/api/*)        │  │  (mutations)    │ │
│  └───────────────┘  └─────────────────┘  └─────────────────┘ │
└──────────────┬───────────────────┬────────────────┬───────────┘
               │                   │                │
        ┌──────▼──────┐    ┌───────▼───────┐  ┌─────▼──────┐
        │  Supabase    │    │  Azure OpenAI  │  │   Stripe   │
        │  Postgres    │    │  (GPT-4o)      │  │  Billing   │
        │  Auth        │    └────────────────┘  └────────────┘
        │  Storage     │
        └──────────────┘
```

### 2.1 Frontend
- **Next.js 14+ (App Router), TypeScript, Tailwind CSS, shadcn/ui.**
- Server Components for data-heavy views (project list, deliverable viewer); Client Components for the intake wizard and rich-text editor.
- Rich text editing for deliverable review: **implemented as plain heading/paragraph fields (Epic E)** rather than TipTap — our content model is already structured (`{ heading, paragraphs: string[] }[]`, §2.6), and the deliverables generated so far are plain prose with no inline formatting need, so a full ProseMirror-based editor would have added real complexity (HTML↔JSON↔section-schema conversion) for a requirement that doesn't exist yet. Revisit TipTap if/when a deliverable type needs inline formatting (bold, links, tables) that plain fields can't express — the storage shape doesn't need to change either way.
- **UI theme (Sprint 2):** a Fluent 2-inspired look — blue `--brand` (`#0f6cbd`, Fluent's brand blue) plus a blue→purple `--gradient-brand` for hero/personality moments, since this product drafts Microsoft Purview deliverables and should feel like it belongs next to Purview/Learn/the M365 admin centers rather than a generic navy B2B SaaS. Iconography is `@fluentui/react-icons` (Microsoft's own Fluent icon set) rather than a generic icon library — one real icon per service type and deliverable category, used on the dashboard, intake form, services checklist, generate form, and deliverable view.
  - **Real bug found while building this**: `@fluentui/react-icons` components use Griffel (CSS-in-JS) internally and cannot be rendered directly in a Server Component (`Attempted to call __styles() from the server`). Fixed by re-exporting the icon set through a `"use client"` barrel file (`src/components/icons.tsx`).
  - **Second, subtler bug found the same way**: a Server Component that destructures an icon out of an object exported from that `"use client"` barrel (e.g. `const Icon = SERVICE_ICONS[service]`) and renders it itself gets `undefined` at render time — a Turbopack/RSC client-boundary quirk with object-valued exports, confirmed by bisecting a throwaway preview route rather than guessing. The fix is architectural, not a workaround: a Server Component must render `<ServiceIcon service={x} />` (a small "use client" component that does the lookup itself, taking only a plain string prop across the boundary) rather than doing the lookup itself and handing the resolved component reference to JSX. Every Client Component (`GenerateForm`, `ServicesForm`, `DeliverableView`) that does the identical-looking lookup *inside its own client code* is unaffected — the failure mode is specific to a Server Component reaching into a client module's object export.

### 2.2 Backend
- MVP backend logic lives in **Next.js Route Handlers and Server Actions** — no separate service to deploy/operate at MVP scale. All business logic (generation orchestration, template rendering, export) is organized into a `lib/` module layer with no framework dependencies, so it can be extracted into a standalone API service later with minimal churn.
- Background jobs (document generation, export rendering) run via a queue-backed worker (see §2.5) rather than inline in the request, since generation can take up to ~60s.

### 2.3 Database — Supabase Postgres
- Chosen over Azure SQL for MVP because it bundles Postgres + Auth + Storage + row-level security in one managed service, cutting setup time significantly. Schema is standard relational SQL; migrating to Azure SQL later is a schema-compatible port, not a redesign (see ERD.md — no Postgres-only features are used in core tables besides `jsonb`, which maps to `nvarchar(max)` + JSON functions in SQL Server if needed).
- Row-Level Security (RLS) policies scope every table to the owning consultant/account from day one — this is also what makes the eventual multi-tenant Partner/Enterprise tier straightforward.

### 2.4 Auth
- **Supabase Auth** (email/password + Google + Microsoft OAuth) for MVP.
- Target architecture swaps/extends this to **Microsoft Entra ID** (including Entra ID B2C or workforce tenant SSO) once Partner/Enterprise customers need enterprise SSO and conditional access — the auth provider is isolated behind a thin adapter in `lib/auth/` for exactly this reason.
- **Multi-seat teams:** an account can now have more than one `users` row (`role`: `'owner' | 'member'`, `supabase/migrations/0017_multi_seat_teams.sql`). Every account-scoped RLS policy already keyed off `auth_account_id()` (a lookup by `auth.uid()`, not a hardcoded single user), so a second user under the same `account_id` gets full project/deliverable/branding access with no policy changes — the only new RLS is `users_owner_manage_role`, letting an owner update a teammate's role (`auth_is_owner()`, a `SECURITY DEFINER` helper following the same pattern as `auth_account_id()`/`is_platform_admin()`, needed because a policy on `users` can't safely re-query `users` under its own RLS without it). Owner-gated at the application layer via a new `requireAccountOwner()` (`src/lib/auth/session.ts`): team management, billing (Stripe checkout/portal), and account deletion. Everything else — projects, deliverables, generation, export, FileVault, branding, changing your own password — stays available to any user on the account, matching a typical shared-workspace model rather than per-seat permissions.
- **Invite flow:** `handle_new_auth_user()` (the trigger that provisions a row in `public.users`/`public.accounts` for every new `auth.users` row) now branches on `raw_user_meta_data->>'invited_account_id'` — present and valid → join that account as `'member'`; otherwise → the original behavior, a brand-new account as `'owner'`. The invite itself uses Supabase's own `admin.auth.admin.inviteUserByEmail()` (no separate email-sending provider needed — same transactional-email path signup/OAuth already uses), with `invited_account_id` passed as user metadata and `redirectTo` pointing at the existing `/auth/callback?next=/set-password` — no changes needed to `/auth/callback` itself, since it already redirects to whatever `next` says once it exchanges the invite code for a session. `/set-password` reuses `changePasswordAction` rather than a separate code path.

### 2.5 AI Generation Engine
- **Azure OpenAI (GPT-4o or later)** is the target provider, not a generic OpenAI API key — customers in healthcare/gov/defense care about data residency and Microsoft's enterprise data handling terms, and this is a differentiator worth having.
  - **Implemented (Epic D):** provider is isolated behind one function, `src/lib/ai/provider.ts`. It currently defaults to a standard OpenAI API key (`OPENAI_API_KEY`) as a temporary substitute to unblock testing without an Azure subscription, falling back to Azure OpenAI if that key isn't set. **Revert to Azure OpenAI before any real federal customer** (see the design partner profile in PRD §11) — data residency and Microsoft enterprise terms don't apply to a standard OpenAI key. Switching is an env var change, not a code change.
- Generation flow (as implemented):
  1. Route Handler / Server Action receives a "generate deliverable" request.
  2. Assembles a prompt from: intake data (structured) + selected services + deliverable-specific prompt template (versioned, stored in `prompt_templates`) + relevant Knowledge Base snippets. Retrieval is real semantic (pgvector) search layered on the original service/industry tag filter (`src/lib/generation/knowledge-base.ts`, `supabase/migrations/0022_kb_semantic_search.sql`) — the tag filter still gates candidates, embedding similarity ranks within that set, and retrieval falls back to tag-filter-only whenever a real semantic result isn't available (no AI provider configured, or an entry hasn't been embedded yet).
  3. Calls the AI provider, parses the structured response (JSON matching the deliverable's section schema — see §2.6), stores it as a new deliverable version.
  4. Runs through a real queue (below), not synchronously within the request — `runGeneration` itself (`src/lib/generation/run.ts`) never changed shape to get there, exactly as this section originally anticipated.
- **Queue: built** (`src/lib/generation/jobs.ts`, `src/app/api/cron/process-generation-jobs/route.ts`, `vercel.json`). `generation_jobs` rows are enqueued (`status: 'queued'`) and returned immediately; a Vercel Cron tick claims up to 5 queued jobs atomically via a `claim_generation_jobs` Postgres function (`for update skip locked`, so two overlapping ticks can't double-process the same job — `supabase/migrations/0021_generation_job_queue.sql`) and runs them one at a time. No new vendor (no Redis, no Azure Service Bus, no Inngest/Trigger.dev) — built entirely on infrastructure this project already has. The client (`generate-form.tsx`) polls `GET /api/generation-jobs/[id]` every 2.5s per job until all are terminal, then refreshes.
  - **Cron cadence: once a day, not once a minute.** Originally built and shipped at `* * * * *` (Vercel Cron's finest granularity), which deployed and merged clean through PR #45-#50 while silently failing every actual Vercel deployment the whole time — a Vercel Hobby plan only permits daily cron schedules, and this surfaced as a deployment-status failure, not a build or test failure, so it went unnoticed for 6 PRs. Degraded to a fixed daily time (`vercel.json`) as an accepted-for-now trade-off rather than upgrading to a paid Vercel plan: a queued job can now sit up to ~24h before a tick looks at it, and since only 5 jobs process per tick, a queue deeper than that takes multiple *days* to fully drain — a real regression from the sub-minute-queued/few-minutes-total experience this section originally described. Revisit (raise the cadence back up, and re-tune `MAX_JOBS_PER_TICK` against real measured AI-provider latency) once the account is on a plan that supports it.

### 2.6 Deliverable / Document Templates
- **Full Phase 2 deliverable catalog complete (Sprint 2, PRD §9):** Executive Summary, SOW, HLD (MVP), plus DLP Design, Retention Strategy, Sensitivity Labeling Plan, Low-Level Design, Testing Guide, UAT Plan, Rollback Procedures, Operational Runbooks, CAB Request, Change Management Plan, Compliance Report — 14 types total. Each has a **structured template**: a fixed set of sections, each with its own prompt fragment and expected output schema (e.g., `{ heading: string, paragraphs: string[] }[]`). LLD reuses HLD's per-service conditional-section pattern (`requires_service` in `section_schema`, PR/migration 0005); the process-oriented docs (Testing Guide, UAT Plan, Rollback Procedures, Runbooks, CAB Request, Change Management) use fixed sections but instruct the model to itemize content per service in scope within each section, the same pattern SOW uses. Compliance Report carries the strongest disclaimer language in the catalog (explicit "not a certified assessment" language as its own section content, not just a closing caveat) since it's the type most likely to be misread as an actual audit artifact if handled carelessly.
- **Practice-area expansion (PRD §12) — catalog complete:** the platform covered only the design partner's Purview-based Data Security & Compliance practice until now. Adds `cloud_migration`/`app_modernization`/`sharepoint`/`analytics_ai` services and one flagship deliverable per new practice area (Cloud Migration Plan, App Modernization Plan, SharePoint Governance Plan, Data & Analytics Strategy — same 8-section depth as the original Purview design docs). Critically, the 10 templates above that were worded as if every project only ever has Purview services in scope (persona lines like "senior Microsoft 365 security and compliance consultant," HLD's "reference actual Purview capabilities by name") needed generalizing first — otherwise a Cloud-Migration-only project would still get Purview-flavored Core docs. Shipped as version-2 rows (`supabase/migrations/0014_generalize_core_prompts.sql`), following the `prompt_templates` versioning design in `docs/ERD.md` (history kept for the `deliverable_versions` audit trail) rather than an in-place edit. HLD's `section_schema` gained 4 new `requires_service` design sections alongside the original 8, same pattern. `supabase/migrations/0015_practice_area_kb_and_deliverables.sql` then added 12 Knowledge Base entries (3 per new service) and the 4 flagship prompt templates themselves — the catalog is now 18 deliverable types across 12 services / 5 practice areas. Same first-draft-content caveat as the rest of the catalog (PRD §10, not SME-reviewed); not yet exercised through live AI generation.
- AI output is validated against this schema (Zod) before being accepted — this is what keeps generated docs formatted consistently rather than free-form prose, per PRD FR-11.
- **DOCX export:** `docx` npm package (`src/lib/export/docx.ts`), populating a firm-branded template (logo/colors read from `branding`, editable at `/dashboard/settings` — Server Action in `src/app/dashboard/branding/actions.ts`, not a literal `PUT /accounts/me/branding` route). The logo is fetched server-side once (`src/lib/export/logo.ts`) and reused as raw bytes across all three export formats rather than each builder re-fetching the URL; https-only, PNG/JPEG-only, 5MB cap, 5s timeout, and any fetch failure silently omits the logo rather than failing the export.
- **PDF export:** `@react-pdf/renderer` (`src/lib/export/pdf.tsx`) rather than the Puppeteer approach originally planned here — a pure-JS renderer avoids bundling headless Chromium into a Vercel serverless function, a real deployment-size/cold-start risk. Revisit if a future deliverable type needs HTML/CSS fidelity this can't express.
- **Download flow (implemented, Epic E):** `GET /api/projects/{id}/deliverables/{deliverableId}/export?format=docx|pdf|pptx` streams the file directly rather than uploading to Storage first — exports are cheap to regenerate from `deliverable_versions.content` on every request, so the Storage/signed-URL round trip in §2.7 below isn't needed for this. `docs/openapi.yaml` updated to match. Also accepts `&disposition=inline` (PDF only meaningfully) so the FileVault's built-in viewer can embed the file in an `<iframe>` instead of triggering a download.
- **FileVault (`/dashboard/vault`):** a cross-project gallery over every `ready` deliverable (`src/lib/vault/service.ts`), grouped by project, so a consultant doesn't need to open each project individually to find something already generated. Deliberately doesn't introduce its own file storage — it's a browsing UI over the same on-demand export pipeline above, not a second copy of the data. Per deliverable: **View** opens a modal with the PDF in an `<iframe>` (the browser's native PDF viewer supplies print/zoom/download controls — no PDF.js or similar library needed), **DOCX/PDF/PPTX** are direct download links (same export route), and **Email** builds a `mailto:` link containing a direct link to the file rather than actually attaching it — there's no email-sending provider configured in this project (see `.env.example`), and `mailto:` can't attach a generated-on-demand file anyway; the recipient still needs their own QuePilot session to open the link, which the UI states explicitly.
- **PPTX export (Sprint 2):** `pptxgenjs` (`src/lib/export/pptx.ts`) — a title slide, then one or more content slides per section (paragraphs become bullets, split across slides once a section overflows a character budget). This is a slide deck, not a page-for-page reflow of the DOCX/PDF — same content, presentation-appropriate layout. Logo embedding shipped alongside the branding settings UI; `pptxgenjs`'s declared (but, per its shipped bundle, dead-code) `image-size` dependency is pinned to a patched 2.x via `package.json` `overrides` regardless, so the earlier DoS advisory (icon/JXL/HEIF parsers) no longer shows up in `npm audit`.

### 2.7 Storage
- **Supabase Storage** for uploaded assets (firm logos) and any future case where a file needs to persist rather than be regenerated on demand, with per-account access policies. Swappable for Azure Blob Storage later behind a thin storage adapter (`lib/storage/`). Not used for DOCX/PDF exports — see §2.6.

### 2.8 Billing
- **Stripe** subscriptions (Checkout + Customer Portal + webhooks) for Consultant/Professional tiers. **Implemented (Sprint 2).**
- `subscriptions` is read-only for the account under RLS (docs/ERD.md) — writes happen server-side via `src/lib/supabase/admin.ts`, a service-role client that bypasses RLS entirely. This is the app's first use of the service role; used only in the checkout/portal Route Handlers and the Stripe webhook handler (which has no user session to write under at all). Never import this client into a Client Component or otherwise expose the key to the browser.
- Price IDs (`STRIPE_PRICE_ID_CONSULTANT`/`STRIPE_PRICE_ID_PROFESSIONAL`) come from Products/Prices created in the Stripe Dashboard — this app can't provision those itself, same class of external-account dependency as the Azure OpenAI deployment (docs/PRD.md §11).
- Usage limits per tier (PRD FR-16) are **not implemented** — no numeric limits have been decided yet (how many projects/generations per plan). Everything up through Stripe subscription tracking is built; enforcing limits is a separate follow-up once those numbers are chosen. Not to be confused with the flat, plan-independent generation rate cap described below — that's an abuse/cost-runaway guardrail, not a pricing decision.
- **Generation rate limiting:** a flat 50-generations-per-24h ceiling per account (`src/lib/generation/rate-limit.ts`), enforced identically in both entry points into the generation pipeline (the Server Action and the `POST /api/projects/{id}/generate` route). Exists purely so a compromised account or a scripting error can't turn into unbounded AI-provider spend — same ceiling regardless of plan, deliberately not a substitute for FR-16 above.
- **Auth rate limiting:** a basic in-memory sliding-window limiter (`src/lib/rate-limit/edge.ts`) on POST requests to `/login` and `/signup`, applied in `src/proxy.ts`. Known, accepted limitation: state is per edge instance and resets on cold start, so it's a deterrent against a single scripted client, not a hard ceiling under real distributed adversarial load — swap in a shared store (Upstash Redis or similar) before relying on this alone.

### 2.9 Hosting & Ops
- **Vercel** for the Next.js app (frontend + Route Handlers).
- **Sentry** for error tracking (`@sentry/nextjs`, wired via `src/instrumentation.ts`/`src/instrumentation-client.ts`/`src/sentry.*.config.ts`, `next.config.ts` wrapped with `withSentryConfig`), **Vercel Analytics** for basic usage — a lightweight substitute for Application Insights at MVP scale; swap-in candidate for Phase 2 if the product needs Azure-native monitoring for enterprise sales conversations. Sentry no-ops safely until a real `NEXT_PUBLIC_SENTRY_DSN` is set (same "configure when you have the real credentials" pattern as Stripe/OpenAI) — before this, the wiring existed only as an unused env var placeholder with no actual initialization code anywhere.

### 2.10 Knowledge Vault — Expert Knowledge System (EKS) MVP
- **What it is:** a private, searchable institutional-memory feature — each account's own lessons learned, incidents, and reusable scripts, captured by the consultants working the account and surviving past any one consultant leaving. Distinct from, and never mixed with, the existing platform-managed `knowledge_base_entries` (admin-authored, global, powers AI generation prompts). The vault is account-authored content that powers nothing except its own search — no generation path reads from it yet (see Open Technical Questions).
- **Data ownership: private per account, not platform-wide.** `knowledge_vault_entries` and `knowledge_scripts` (`supabase/migrations/0027_knowledge_vault.sql`) are account-scoped tables with plain account-scoping RLS (`account_id = auth_account_id()`, all four verbs) — deliberately **no platform-admin read override**, unlike the admin-write/authenticated-read model on `knowledge_base_entries`. A consulting firm's own captured incidents and lessons learned are exactly the kind of content that shouldn't be visible across tenants or to QuePilot staff by default; this is a stricter isolation boundary than anything else in the schema, chosen deliberately rather than inherited from the existing pattern.
- **Schema shape:** `knowledge_vault_entries` unifies "Lessons Learned" and "Incidents" as one table with an `entry_type` discriminator (`'lesson_learned' | 'incident'`) rather than two near-duplicate tables — the two share the large majority of fields (title, symptoms, root cause, resolution, environment, tags, service), and this is the same discriminated-table convention `generation_jobs` already uses for covering every deliverable type via one column. `knowledge_scripts` is a separate table (name/description/content/risk_level/script_type across 7 categories — PowerShell, Graph API, KQL, JSON, Terraform, Bicep, ARM template) since a script's shape genuinely doesn't overlap with a narrative entry's. Both carry `embedding vector(1536)`, an HNSW index, and a `version` int bumped on edit — the identical CRUD-with-embedding-regen pattern `knowledge-base/service.ts` already established (best-effort regeneration; only overwrites the stored embedding if regeneration succeeds, never blocks the save on an AI-provider hiccup).
- **Search (`src/lib/vault/search.ts`):** two `security invoker` (not `definer`) match RPCs, `match_knowledge_vault_entries`/`match_knowledge_scripts` — invoker rather than definer so the RPC runs as the calling user and inherits RLS automatically, rather than needing an explicit account-id parameter that could be passed wrong. Same retrieval-with-fallback shape as `knowledge-base.ts`'s `trySemanticSearch`: embed the query, call the RPC, fall back to a pure term-match ranker (`rankByTextMatch`) whenever no AI provider is configured or the embedding call fails. Entries and scripts return as two separate ranked lists rather than one blended list — there's no "AI-generated content" tier in this MVP to blend real captured knowledge against, unlike Knowledge Base retrieval's tag-filter-then-semantic-rank layering.
- **"What would [teammate] do?" filter:** a generic `filterByAuthorId()` pure filter (`src/lib/vault/search.ts`) backing an author dropdown on both vault list pages, populated from the existing `listTeamMembers()` — not hardcoded to any one person, works for any account member.
- **Dashboard:** one Card on the existing `/dashboard/metrics` page (`summarizeVaultMetrics()`, `src/lib/metrics/vault.ts`) — total entries/scripts, a lessons-learned-vs-incidents split, entries by service. Real counts only, no scored heuristic, added to the existing page rather than a new one — same incremental-Card pattern Engagement Health and Delivery Risk followed onto this page.
- **MVP scope vs. the full brief:** the originating product brief specced 13 modules (SOP/playbook generation, a troubleshooting/architecture-advisor reasoning layer, an engagement assistant, institutional-knowledge-risk scoring, 5 dashboards). MVP ships only the core capture-and-search loop — the unified vault, scripts, search, the author filter, and one dashboard slice. The rest is staged into V2/V3/Enterprise phases below, sequenced deliberately *after* there's real vault content to build on: scoring an institutional-risk signal or generating a dashboard against empty/seed data would be fabricating signal, the same reason Delivery Risk and Security Score are documented heuristics over real data only rather than guessed thresholds.

### 2.11 Automation Center MVP

- **What it is:** three tools built on top of the Knowledge Vault (§2.10) rather than a separate knowledge system — **Code Auditor** (paste a script, get a structured multi-category review + scorecard), **Code Creator** (describe what you need, answer a fixed environment-aware requirements form, get a generated script), and **Coding Standards Templates** (each account's own "how WE write PowerShell" definition, not generic language documentation). Two of the three features the originating brief asked for — Script Vault and Lessons Learned — already existed from §2.10's EKS build; this MVP's genuinely new scope is the two AI tools, the Standards Templates, and Environment Profiles.
- **Three-layer knowledge philosophy, made concrete:** Layer 1 (public language/API docs) is never stored — Code Auditor/Code Creator rely on the model's own trained knowledge, same as every other generation feature in this app; Layer 2 is `coding_standards`, this account's own template; Layer 3 is the existing `knowledge_vault_entries` lessons-learned/incidents, pulled into Code Creator's prompt via the existing `searchVault` (§2.10) rather than a second embedding index.
- **Data ownership: `coding_standards` is private per account**, same RLS shape as `knowledge_vault_entries`/`knowledge_scripts` (`account_id = auth_account_id()`, all four verbs) — one row per `(account_id, script_type)`, a `required_elements text[]` checklist plus freeform `notes`, `version` bumped on edit. `automation_requests` is the AI-call log for both tools (`supabase/migrations/0029_automation_center.sql`) — append-only (select+insert RLS only), doubling as both the rate-limit counter and a user-facing history, the same dual role `generation_jobs` already plays for deliverable generation.
- **A separate rate limiter, not shared with deliverable generation.** `src/lib/automation/rate-limit.ts`'s `assertUnderAutomationRateLimit` counts `automation_requests` against its own 50/day ceiling, independent of `generation/rate-limit.ts`'s deliverable cap — confirmed design decision: Code Auditor/Code Creator are a fast, single-call usage pattern, not per-project batching, and shouldn't eat into a customer's deliverable-generation budget.
- **Validation pipeline reuses `generation/run.ts`'s three-layer pattern exactly:** `JSON.parse` (try/catch → typed error) → Zod schema validate (`codeAuditResultSchema`/`generatedScriptSchema`, `.safeParse` → typed error) → a structural sanity check via `.superRefine` (Code Auditor's scorecard can't score a category ≥90 while also raising a critical/high finding in that same category — the same class of drift `headingsMatchRequiredSections` catches for deliverables). Both tools also reuse `generation/prompt.ts`'s rotating-tag prompt-injection mitigation (SEC-02) around every piece of account-authored free text reaching the prompt — pasted code, the description/requirements fields, a standard's freeform notes, and pulled-in lessons-learned content each get their own `<untrusted_..._{random}>` tag.
- **Environment Profiles are hardcoded platform reference data, not a table.** `src/lib/domain/environment-profiles.ts` — five profiles (Commercial, GCC, GCC High, DoD, Azure Government), each with a Graph base URL, auth guidance, and compliance notes — the same "static Microsoft platform knowledge, not a per-customer setting" reasoning `labels.ts` already applies to `SERVICE_LABELS`. Feeds Code Creator's prompt assembly and its requirements form. This is genuinely new structured data — GCC/GCC High/DoD context existed elsewhere in this app only as unstructured prose (KB content, prompt-template instructions) before this build, never as an enum or form field.
- **Code Creator's requirements step is a fixed form, not AI-generated dynamic questions** (confirmed design decision) — a two-step describe → requirements → result client flow (`src/app/dashboard/automation/creator/creator-form.tsx`) using a discriminated-union local state, mirroring `mfa-section.tsx`'s `EnrollState` pattern (the closest existing multi-step-flow precedent; no generic wizard component exists in this codebase).
- **Code Creator's chalkboard view (added after MVP)** — the generate step no longer calls a Server Action; it `fetch()`s `POST /api/automation/creator/stream`, a Route Handler returning a real streamed `Response` (Server Actions can't stream back to the client), and renders the response arriving live on a themed "digital chalkboard" (`src/components/ui/chalkboard.tsx`) with an animated QuePilot-robot mascot. This meant changing the AI contract for this one feature from JSON (`generateCompletion`, `response_format: json_object`) to plain delimiter-marked text (`streamCompletion` in `src/lib/ai/provider.ts`, `%%%QUEPILOT_SCRIPT%%%`/`_NOTES%%%`/`_ROLLBACK%%%` markers parsed by `src/lib/automation/stream-format.ts`) — streamed JSON would show escaped quotes/newlines mid-stream instead of readable code as it's written. `src/lib/automation/creator.ts` split the old single `runCodeCreator` call into `prepareCodeCreatorRequest` (everything before the AI call: standard/lessons lookup, prompt assembly, the `automation_requests` insert) and `finalizeCodeCreatorRequest` (parse + persist once the stream ends), so the route handler can stream the middle part directly to the client. Every other Automation Center mutation still follows the standard `FormData` + `useActionState` Server Action template — this is the one place that doesn't, for the same reason the old JSON-based version already wasn't a `<form action>` (a typed input object, not `FormData`), now compounded by needing a streamed `Response` a Server Action can't give.
- **"Promote to Script Vault"** writes a generated script into the existing `knowledge_scripts` table via the existing `createVaultScript`, now taking an optional `source` param (`'manual' | 'ai_generated' | 'promoted'`, migration 0029) — the first writer of that column and its sibling `is_approved_pattern` flag (the brief's "Approved Patterns" concept; a flag on an existing script, not a parallel table).
- **Approved Patterns:** a checkbox on both the Script Vault form (`script-form.tsx`) and the "Save to Script Vault" promote form (`creator-form.tsx`), framed with the brief's own qualifying questions ("Was this useful? Would you reuse it?"); an "Approved Patterns only" filter and badge on the Script Vault list page (`knowledge-vault/scripts/page.tsx`), same query-param pattern as the existing teammate filter. Code Creator's *generation* step reading from approved patterns as reference material (the way it already reads lessons learned) is still open — see the roadmap below.
- **Hub page + nav:** `/dashboard/automation` ties Script Vault, Code Auditor, Code Creator, and Coding Standards together as one sidebar tab (`Automation Center`), per the brief's own framing — a flat top-level nav entry rather than a group, since the hub page itself is the landing destination the brief asked for.
- **MVP scope vs. the full brief:** live Layer-1 documentation fetching (the brief's "pull the latest Graph docs" idea) needs a real doc-search/fetch integration this MVP doesn't have — same reasoning as every other generation feature here, it relies on the model's own trained knowledge rather than fabricating a live-fetch architecture with nothing behind it. "Which standards get violated most often" analytics and true AI-driven dynamic Code Creator follow-up questions are deferred to V2 for the same reason Knowledge Vault's own deferred items are (§2.10) — no real usage data yet to build a signal against.

## 3. Target (Enterprise) Architecture — Phase 2+

| Layer | MVP | Target |
|---|---|---|
| Frontend | Next.js on Vercel | Next.js (same) |
| Backend API | Next.js Route Handlers | Dedicated .NET API (extracted from `lib/`) |
| Database | Supabase Postgres | Azure SQL (schema-compatible migration) |
| Auth | Supabase Auth | Microsoft Entra ID (workforce + B2C) |
| Storage | Supabase Storage | Azure Blob Storage |
| AI | Azure OpenAI | Azure OpenAI (unchanged) |
| Queue/Cache | pgmq / jobs table | Redis (job queue + caching) |
| Monitoring | Sentry + Vercel Analytics | Application Insights |
| Tenant integration | none | Microsoft Graph + Purview read APIs for tenant discovery |

The migration path is intentionally incremental: swap one adapter (`lib/auth`, `lib/storage`, `lib/db`) at a time behind stable interfaces, rather than a rewrite.

## 4. Data Model

See `docs/ERD.md` for the full entity-relationship diagram and table definitions. Core entities: `accounts`, `users`, `projects` (intake data), `services_in_scope`, `deliverables`, `deliverable_versions`, `knowledge_base_entries`, `prompt_templates`, `knowledge_vault_entries`, `knowledge_scripts`, `coding_standards`, `automation_requests`.

## 5. API Surface

See `docs/openapi.yaml` for the MVP REST API specification covering auth-adjacent account endpoints, projects, deliverables, generation jobs, and knowledge base admin endpoints.

## 6. Security & Compliance Considerations (MVP)

- No customer tenant credentials are collected or stored in MVP (no live Graph/Purview access yet) — this significantly reduces MVP security scope.
- All project/customer data protected by Postgres RLS scoped to account ownership.
- Secrets (Azure OpenAI keys, Stripe keys) managed via Vercel environment variables, never committed.
- Generated deliverables always carry the AI-disclaimer (PRD FR-7) — a product requirement with a compliance/liability rationale, not just UX.
- SOC 2 Type II is out of scope for MVP but should be scoped as a Phase 2/3 project once Enterprise-tier sales conversations require it.
- **Fixed: PostgREST filter injection in knowledge-base retrieval.** `getRelevantKnowledgeBaseEntries` previously built a Supabase `.or()` filter via raw template-literal interpolation of the project's free-text `industry` field (no closed enum at the Zod layer — "Other" is a legitimate value). A value containing Postgrest filter syntax (comma, dot, parenthesis) reached the query unescaped. Found during a red-team review; fixed by fetching matching `service_type` rows and filtering by `industry` in application code instead of a filter string, removing the injection vector entirely rather than trying to escape it correctly. Blast radius was always limited to `knowledge_base_entries` (public-to-every-authenticated-user content, not account-scoped) — that was the query's own nature, not a control, and the same pattern anywhere near an account-scoped table would have been a real cross-tenant leak.
- **Terms of Service and Privacy Policy** now exist (`/terms`, `/privacy`) — first drafts, explicitly labeled as not yet reviewed by counsel, with bracketed placeholders for the legal entity name, governing jurisdiction, and contact address. Linked from login, signup, and Settings. Previously there was no legal basis published anywhere for processing a customer's (or their client's) data — a hard blocker for onboarding anyone beyond the design partner.

## 7. Open Technical Questions

- Vector search for Knowledge Base retrieval: needed once KB grows past what tag-filtering can serve well — likely `pgvector` on Supabase before introducing a dedicated vector DB.
- Real-time collaborative editing on deliverables (multiple reviewers) is out of scope for MVP; TipTap supports it later via Yjs if needed.
- Exact boundary for when to extract the backend into a dedicated .NET service (team size, request volume, or specific enterprise security requirement — whichever comes first).
- **Knowledge Vault (§2.10) roadmap beyond MVP — Phase 2 (EKS V2) shipped:** SOP Generation Engine + Playbook Library (`sops`/`playbooks`, migrations 0032/0033, a shared `generateStructuredDoc()` AI-generation core, each with its own manual-entry-first CRUD, account-scoped RLS, and later semantic search); a Troubleshooting Engine and Architecture Advisor (`src/lib/eks/*`, migration 0034's `eks_requests`/`eks_request_vault_entries`, its own 50/day rate limit) as AI-reasoning layers built on existing vault search rather than new schema, exactly as originally sketched; Institutional Knowledge Risk scoring (`src/lib/metrics/institutional-risk.ts`, thresholds documented as a judgment call — 50%/75% single-author share, minimum 3 items before scoring); entry-to-entry relations (`sop_vault_entry_links`/`playbook_vault_entry_links`, migration 0036, a curated "related vault entries" multi-select, plus `eks_request_vault_entries`' automatic cross-referencing, plus a Module 1 gap-closure pass adding the reverse direction — a vault entry now browses the SOPs/playbooks/scripts that reference it — and real FileVault-backed attachments on vault entries themselves); a project-closure Knowledge Capture gate (`projects.status`, migration 0031) requiring at least one vault entry before a project can close. Dashboard IA changed from the originally-sketched 4 separate pages to tabs on the existing `/dashboard/metrics` page (Confirmed Decision 3) — Knowledge/Consultants/Risk tabs, each account-scoped only (no `/admin/metrics` equivalent, preserving this content's private-per-account RLS boundary); unified search (`src/lib/search/knowledge.ts`) adds Historical Projects, Playbooks, and SOPs to the existing vault search, still separate ranked lists per type rather than one blended feed. An Engagement Assistant remains not built — still largely covered by the existing deliverable catalog, worth extending rather than rebuilding, same conclusion as before. **"Most valuable content" usage tracking — done.** `content_views` (migration 0046/0047) records who opened a lesson/incident, script, SOP, or playbook, deduped to one row per viewer per day so a refresh doesn't inflate the count; distinct from the cross-reference counts above, which only ever answered "was this cited by other content," never "did a person look at it." Surfaced two ways: a "Viewed N times" line on each entry's own edit page, and a "Most viewed" / "Never viewed" pair of sections on the Knowledge dashboard tab (`src/lib/metrics/content-views.ts`), the latter modeled directly on `usage.ts`'s existing `topKbEntries`/`unusedKbEntries` pattern but for actual opens instead of deliverable citations. "Hours saved" (no real time-tracking data exists anywhere in this schema to anchor it on) remains deliberately unscheduled.
- **Automation Center (§2.11) roadmap beyond MVP:** live Layer-1 documentation fetching, once a real docs search/fetch integration exists; Code Creator's own generation step reading from `is_approved_pattern` scripts as reference material the way it already reads lessons learned (the "Approved Patterns" toggle, filter, and promote-flow wiring shipped — this is specifically about generation *building from* them, not just marking/browsing them); "which standards get violated most often" analytics, once there's enough real `automation_requests`/audit history to compute a real signal from; true AI-driven dynamic clarifying questions for Code Creator, if the fixed-form v1 proves too rigid in practice.
- **Locked-in roadmap: status against the original 13-module EKS brief, updated after EKS V2 (this is the canonical tracking list — check here before re-scoping any of these modules):**
  - Module 1 (Lessons Learned Vault) — **done, gap-closure pass.** `customer_size` field, the reverse of the `sop_vault_entry_links`/`playbook_vault_entry_links` relation (a vault entry's own page now lists the SOPs/playbooks/scripts that reference it), and real FileVault-backed attachments on vault entries.
  - Module 2 (Incident KB) — **done** (unified into `knowledge_vault_entries` via `entry_type`, pre-EKS V2).
  - Module 3 (SOP Generation Engine) — **done, EKS V2.** `sops` table + `generateStructuredDoc()`-backed generation flow.
  - Module 4 (Purview Playbook Library) — **done, EKS V2.** `playbooks` table, mirrors SOPs.
  - Module 5 (Script Knowledge Center) — **done** (pre-EKS V2).
  - Module 6 (Troubleshooting Engine) — **done, EKS V2.** `src/lib/eks/troubleshoot.ts` — real problem-statement-in, `searchVault`-grounded similar-issues, AI-reasoned causes/flow/commands out.
  - Module 7 (Architecture Advisor) — **done, EKS V2.** `src/lib/eks/architecture-advisor.ts`, reuses the existing diagram-generation machinery.
  - Module 8 (Engagement Assistant) — **done, gap-closure pass.** The existing deliverable catalog already covered most of this ground; the specific gap (questionnaires, risk registers, PPT decks not built) is closed by adding 3 new deliverable types to that same catalog rather than a new subsystem — `discovery_questionnaire` (17 sections, 4 always + 12 per-service + 1 always, same exhaustive per-service gating pattern as High-Level Design), `risk_register` (18 sections, same shape), `client_presentation` (6 always-present sections, written for short slide-style bullets — exported through the existing PPTX/DOCX/PDF builders unchanged, no new export code). Migrations 0040 (enum) + 0041 (prompt templates).
  - Module 9 (Knowledge Capture Workflow enforcement at project closure) — **done, EKS V2.** `projects.status`/`closed_at` (migration 0031) — closing a project requires at least one linked vault entry.
  - Module 10 (Semantic Search) — **done.** Unified `searchKnowledge()` now covers Historical Projects (`src/lib/projects/search.ts`, text-filtered against `customer_name`/`industry`/`licensing_tier`/`compliance_notes`/`geographic_locations`/services in scope — all real fields already in the schema), Playbooks (text-only), and SOPs (real embeddings via `match_sops`) alongside entries/scripts, plus Microsoft Docs (`src/lib/search/microsoft-docs.ts`), in the original brief's Historical Projects → Playbooks → Lessons Learned/Incidents → Scripts → SOPs → Microsoft Docs order. Microsoft Docs is a real, live integration against Microsoft's own official unauthenticated MCP server (`https://learn.microsoft.com/api/mcp`, tool `microsoft_docs_search`, via `@modelcontextprotocol/sdk`) — not a fabricated stand-in — but this sandbox's network egress blocks `learn.microsoft.com`, so the live call itself has never round-tripped from here. The response parser is written defensively (structured JSON first, a markdown-link-extraction fallback second) precisely because the exact tool response shape couldn't be confirmed ahead of time, and its logic is fully unit-tested (`microsoft-docs.test.ts`) independent of the network call. `searchMicrosoftDocs()` returns `null` (not `[]`) on any connect/call/timeout/unrecognized-shape failure, rendered as "unavailable" rather than "no results" — see `docs/validation-checklist.md` for the verification gap this leaves until it's confirmed live in production.
  - Module 11 ("What Would Michael Do?") — **done.** Beyond the author-filter dropdown, `/dashboard/consultants/[userId]` (per-content-type counts + a by-service `BarChart`) and an org-wide "Consultants" dashboard tab ranking every teammate's combined contributions, the profile page now has the personal-memory query itself: `searchConsultantKnowledge()` (`src/lib/team/contributions.ts`) reuses `searchKnowledge()` unchanged (ranks account-wide first, same as every other search entry point) then filters each list to just this one author with the same generic `filterByAuthorId` the page already used for its counts — "what would this person say about X," not just "what has this person captured."
  - Module 12 (Dashboards) — **done, still tabs rather than the 5 named routes as originally specced (Confirmed Decision 3).** `/dashboard/metrics` now has Overview/Executive/Knowledge/Consultants/Risk tabs. The new Executive tab is a leadership rollup built entirely from signals this app already computes elsewhere — portfolio-wide Engagement Health and Delivery Risk (both reused unchanged from Overview), the account's Security Score (previously visible only on Settings), and a "needs attention" worklist that joins Engagement Health with Delivery Risk per project, worst first. No separate "Purview-issues" dashboard: that would need real issue/finding tracking this app doesn't have, and fabricating one would repeat the same mistake this app's docs already warn against elsewhere (Automation Center's Layer-1 docs fetch) — real signals surfaced clearly beats an invented one.
  - Module 13 (Institutional Knowledge Risk scoring) — **done, EKS V2.** `computeInstitutionalRisk`/`getInstitutionalRiskSummary`, surfaced on the Risk dashboard tab alongside an aging-content check (no edit in 6 months).
  - **Workflow engine — done, scoped to a deliverable approval workflow.** "Workflow engine" had no further spec behind it in this roadmap; scoped (with the user) to the concrete gap this app actually had: a deliverable went straight from "AI generated it" to "export it to the client" with nobody in between. Migration 0044 adds `review_status` (`not_submitted -> in_review -> approved | changes_requested`, the last looping back to `in_review` on resubmission) as a second lifecycle on `deliverables`, independent of the existing generation-pipeline `status`. `src/lib/generation/review.ts` holds the pure, unit-tested state-transition rules (`canSubmitForReview`/`canDecideReview`/`canExportForClient`); `src/app/dashboard/[projectId]/review-actions.ts` are the three Server Actions (submit/approve/request changes) enforcing them server-side, not just in the UI. It's a real gate, not just a status label: the export route blocks the client-facing "attachment" disposition until `approved` (the internal "inline" preview stays open — a reviewer has to see a draft to approve it), and editing a deliverable resets the whole workflow back to `not_submitted` since a stale approval can't carry over onto unreviewed content. Flat RBAC stays flat — no new "reviewer" role, any teammate can submit or decide, including the same person doing both.
  - **"Que" — a single-shot platform assistant — done.** A chat-style surface on the dashboard home page (`/dashboard`, under "New project"), asked for directly by the user. Deliberately **not** open-ended multi-turn chat: an earlier full-conversation design (persisted `assistant_conversations`/`assistant_messages` tables, growing context on every turn) was built, then explicitly walked back once its AI cost profile was flagged as a real concern — a long-running conversation both grows its own prompt every turn and has no natural bound on turn count, unlike everything else in this app's AI-generation surface. Ask Que is bounded instead: one question in, one grounded answer out, joining the `eks_requests` family as a third `feature` value (`'ask_que'`, migration 0048) right alongside the Troubleshooting Engine and Architecture Advisor — no new conversation-storage schema at all. Every question runs `searchMicrosoftDocs()` and `searchVault()` in parallel against the account's own Knowledge Vault, feeds both into the prompt alongside a hand-written "what this platform does" primer (`src/lib/assistant/platform-knowledge.ts`), and validates a single `{reply: string}` JSON field. "Generate an SOP from this" is a separate, explicit follow-up action (its own API call, its own rate-limit check) bounded to exactly the one question/answer pair just shown — never a full transcript — that delegates entirely to the existing `generateSop()` engine, so a Que-generated SOP is a completely ordinary SOP afterward (searchable, exportable, cross-referenceable, view-tracked, audit-logged). Shares the Troubleshooting Engine/Architecture Advisor's existing 50/day `eks_requests` rate limit rather than getting its own budget, the same way those two already share it. Closing this also exposed a real gap: SOPs had no export-to-Word at all before this (every deliverable type already had one) — `src/lib/export/sop-docx.ts` + `/api/sops/[id]/export` fill it, DOCX only for now.
  - **Still not built at all:** a real RBAC/permission model beyond flat owner/member (Confirmed Decision 4 — deliberately deferred, not forgotten); a knowledge lifecycle/aging *policy* (the Risk tab's aging-content list is a read-only signal, not an enforced review/archive cadence); true multi-turn conversation memory for Que (considered and explicitly rejected for cost-predictability reasons — see above); PDF/PPTX export for SOPs (DOCX only, matching what was actually asked for).
